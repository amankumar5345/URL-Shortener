import React from 'react';

const Header: React.FC = () => (
    <header className="header">
        <h1>URL Shortener</h1>
        <p>Instantly shorten links and save results locally.</p>
    </header>
);

export default Header;
